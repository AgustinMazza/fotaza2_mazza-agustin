extends layout.pug

include mixins/publicacion.pug

block content
  h1.text-center.mb-4= titulo
  
  if publicaciones.length === 0
    div.alert.alert-info.text-center No hay publicaciones para mostrar.
  else
    div.row
      each pub in publicaciones
        +tarjetaPublicacion(pub)